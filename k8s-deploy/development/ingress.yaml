apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: parliamentary-questions
  namespace: parliamentary-questions-development
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  tls:
  - hosts:
    - parliamentary-questions-development.apps.live-1.cloud-platform.service.justice.gov.uk
  rules:
  - host: parliamentary-questions-development.apps.live-1.cloud-platform.service.justice.gov.uk
    http:
      paths:
      - path: /
        backend:
          serviceName: parliamentary-questions-service
          servicePort: 3000
