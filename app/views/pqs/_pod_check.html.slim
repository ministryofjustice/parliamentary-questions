h4 Date sent to POD
p.text= @pq.pod_waiting.strftime('%d/%m/%Y %H:%M') unless @pq.pod_waiting.nil?
= f.check_box_gds :pod_query_flag
.form-group
  label.form-label for="pod_clearance"  Date cleared by POD
  span.input-group.date.datetimepicker data-date-format=("DD/MM/YYYY HH:mm") 
    input#pod_clearance.form-control name="pq[pod_clearance]" type="text" value=(@pq.pod_clearance.strftime('%d/%m/%Y %H:%M') unless @pq.pod_clearance.nil?) /
    span.input-group-addon.glyphicon.glyphicon-calendar
