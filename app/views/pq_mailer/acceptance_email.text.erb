
<%= @template_params[:ao_name] %>,
You have accepted responsibility for drafting an answer to PQ <%= @template_params[:uin] %>


<============================================================================
<%= @template_params[:text] %>.
============================================================================


<div>

<% if @template_params[:member_name].present? %>
	Question asked by <%= @template_params[:member_name] %>
	<% if @template_params[:house_name].present? %>
		(<%= @template_params[:house_name] %>)
	<% end %>
<% end %>
<% if @template_params[:mpname].present? %>
To be answered by <%= @template_params[:mpname] %>
<% end %>
<% if @template_params[:date_to_parliament].present? %>
Due back to Parliament by <%= @template_params[:date_to_parliament] %>
<% end %>
<% if @template_params[:internal_deadline].present? %>
Deadline to return your draft response is <%= @template_params[:internal_deadline] %>
<% end %>

You MUST use the email list below to ensure that all relevant people are cc’d.:-

<%= @template_params[:cc_list]  %>

You will need to liaise with <%= "finance (#{ @template_params[:finance_users_emails]}) and" if !@template_params[:finance_users_emails].empty?  %> your relevant press officer (<%=@template_params[:press_email] %>) before you send your draft response. For drafting templates and guidance on how to respond, please refer to the intranet PQ guidance by using the following link.
	http://intranet.justice.gsi.gov.uk/ministers-parliament/working-with-parliament/parliamentary-questions/index.htm

If you have further queries about drafting your response, please contact the PQ team <%= Settings.mail_reply_to %>


<%= render 'shared/mailer/email_signature_plain' %>
