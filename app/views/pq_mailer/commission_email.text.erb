<%= @template_params[:ao_name] %>,
You have been allocated the the PQ <%= @template_params[:uin] %>
============================================================================
<%= @template_params[:question] %>.
============================================================================
Asked by <%= @template_params[:member_name] %> (<%= @template_params[:house] %>)

You need to accept or reject the question, by clicking the link below or cutting and pasting this url into a web browser, before 2pm today.
 <%= url_for(controller: "assignment", action: "index", uin: @template_params[:uin], token: @template_params[:token], entity: @template_params[:entity], :host => root_url )%>

If you accept the question, you will be sent a further email with all the relevant guidance to draft an answer.

If you have any questions, please contact the PQ team at <%= Settings.mail_reply_to %>

<%= render 'shared/mailer/email_signature_plain' %>